#! /bin/bash
current_path="$(pwd)"
tail="${current_path#/*/genopedia/}"
RAILS_ROOT="${current_path%/$tail}"
cd "$RAILS_ROOT/app/services/benchmarking/scripts"
RAILS_ROOT="$RAILS_ROOT" PROGRAM_NAME="run_<%= file_name %>" OUTPUT_FILENAME="<%= file_name %>_results" TEST_CLASS="<%= class_name %>" ./run_tests.sh "$@"
